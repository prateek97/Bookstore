<%= form_for(food_item) do |f| %>
    <% if food_item.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(food_item.errors.count, "error") %> prohibited this food_item from being saved:</h2>

          <ul>
            <% food_item.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: "form-control-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :description, class: "form-control-label" %>
      <%= f.text_area :description, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :price, class: "form-control-label" %>
      <%= f.text_area :price, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :section, class: "form-control-label" %>
      <%= f.text_field :section, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :image_url, class: "form-control-label" %>
      <%= f.text_field :image_url, class: "form-control" %>
    </div>

    <%= f.submit 'Submit', class: 'actions btn btn-success-outline' %>

<% end %>
